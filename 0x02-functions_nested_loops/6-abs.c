#include <stdio.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @int: input
 *
 * Return: (0)
 */

int _abs(int)
{
	char l;
	int n;

	l = '\n';
	n = (int  * -1);
	_putchar(n);
	_putchar(l);
	return (0);
}
